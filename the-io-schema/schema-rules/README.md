# Schema Rules

How schemas compose, resolve, and validate. Evidence-based from `io-js2/src/schema` and `io-js2/src/schema-v2` utilities.

- Resolution: `$schema` and `@var` lookup
- Composition: reuse, extension patterns
- References: scoping and precedence
- Validation flow: parse → validate → load → stringify
