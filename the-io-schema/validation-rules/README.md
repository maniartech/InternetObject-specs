# Validation Rules

Validation model and semantics used by the IO schema system.

## Topics
- Parse vs Validate vs Load
- Optional (?) and Nullable (*)
- Type inference
- Error handling
- Custom validators

> Evidence from `io-js2/src/schema/validation/` and schema-v2 `types/` interface.
